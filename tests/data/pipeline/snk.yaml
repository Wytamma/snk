annotations:
  times: 
    default: 1
    type: int
    help: Number of times to repeat the message
  text:
    default: "Hi from snk.yaml!"
    type: str
    help: "A string to save to a file"
  test:
    test_bool:
      default: true
      type: bool
      help: "A boolean to test"
    test_int:
      default: 1
      type: int
      help: "An integer to test"
    another:
      test:
        default: "test"
        type: str
        help: "A string to test"
    test_list:
      default: 
        - 1
        - 2
        - 3
      type: list
      help: "A list to test"
    test_null:
      default: null
      help: "A null to test"
symlink_resources: true
logo: "MyPipeline"
tagline: "\nA pipeline to test the pipeline logo and tagline"
font: "cybermedium"